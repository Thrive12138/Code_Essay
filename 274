class Solution {
    public int hIndex(int[] citations) {
        int len = citations.length;
        int flag = 0;
        Arrays.sort(citations);
        for(int i = len - 1; i >= 0; i--) {
            if(citations[i] > len - i) {
                flag++;
            } else {
                return Math.max(citations[i], flag);
            }
            
        }
        return flag;
    }
}
