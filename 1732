class Solution {
    public int largestAltitude(int[] gain) {
        int height = 0, ans = 0;
        for(int i = 0; i < gain.length; i++) {
            height += gain[i];
            ans = Math.max(ans, height);
        }
        return ans;
    }
}
